
import 'package:neumorphism_flutter/lightsourcewidget.dart';

class LightSourceModel {
  final LightSourcePosition position;
  final bool isEnabled;

  LightSourceModel(this.position, this.isEnabled);
}
