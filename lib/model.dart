import 'package:flutter/material.dart';

class ContainerModel {
  String borderRadius;
  String blurRadius;
  String spreadRadius;
  String intensity;
  Offset offset;

  ContainerModel(
      {this.borderRadius, this.blurRadius, this.spreadRadius, this.intensity,this.offset});
}
